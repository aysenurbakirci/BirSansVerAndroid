<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.birsansverapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BirSansVerAPP">
        <activity android:name=".IlanlarimActivity"></activity>
        <activity android:name=".IlanlarActivity" />
        <activity android:name=".IlanBilgileriActivity" />
        <activity android:name=".YeniIlanActivity" />
        <activity android:name=".UygulamaMenuActivity" />
        <activity android:name=".KayitActivity" />
        <activity android:name=".GirisActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>